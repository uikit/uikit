import Position from './position';
import Svg from './svg';
import Toggle from './toggle';

export default function (UIkit, _) {

    UIkit.use(Position);
    UIkit.use(Svg);
    UIkit.use(Toggle);

}
